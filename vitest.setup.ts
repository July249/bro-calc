import { beforeAll, afterAll, afterEach } from 'vitest'

// 각 테스트 후 정리
afterEach(() => {
  // 필요한 정리 작업
})

// 전역 테스트 타임아웃 설정
beforeAll(() => {
  // 필요한 전역 설정
})

// 모든 테스트 완료 후 정리
afterAll(() => {
  // 필요한 정리 작업
})
